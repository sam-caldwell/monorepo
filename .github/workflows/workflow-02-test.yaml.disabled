---
name: workflow-02-test

on:
  workflow_run:
    workflows:
      - foo
      - "workflow-00-setup"
      - "workflow-01-lint"
    types:
      - completed
  push:
    branches:
      - foo
#      - main
    tags:
      - foo
#      - v*
  pull_request:
    branches:
      - foo
#      - main

jobs:
