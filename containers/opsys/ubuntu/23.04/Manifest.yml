---
clean:
  enabled: true
  steps:
    - docker rmi -f opsys/ubuntu:23.04 || true

build:
  enabled: true
  steps:
    - docker build --tag opsys/ubuntu:23.04 --file containers/language/opsys/23.04/Dockerfile .

test:
  enabled: false
