---
clean:
  enabled: true
  steps:
    - docker rmi -f opsys/ubuntu:20.04 || true

build:
  enabled: true
  steps:
    - docker build --tag opsys/ubuntu:20.04 --file containers/language/opsys/20.04/Dockerfile .

test:
  enabled: false
