---
clean:
  enabled: true
  steps:
    - continueOnError: true
      command: "docker rmi -f language/java:21"

build:
  enabled: true
  steps:
    - continueOnError: false
      command: "docker build --tag language/java:21 --file containers/language/java/21/Dockerfile ."

test:
  enabled: false

