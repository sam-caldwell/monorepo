---
clean:
  enabled: true
  steps:
    - continueOnError: true
      command: "docker rmi -f language/python:3"

build:
  enabled: true
  steps:
    - continueOnError: false
      command: "docker build --tag language/python:3 --file containers/language/python/3/Dockerfile ."

test:
  enabled: false
