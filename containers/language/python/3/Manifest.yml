---
clean:
  enabled: true
  steps:
    - docker rmi -f language/python:3 || true

build:
  enabled: true
  steps:
    - docker build --tag language/python:3 --file containers/language/python/3/Dockerfile .

test:
  enabled: false
