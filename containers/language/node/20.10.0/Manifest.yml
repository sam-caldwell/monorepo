---
clean:
  enabled: true
  steps:
    - docker rmi -f language/node:20.10.0 || true

build:
  enabled: true
  steps:
    - docker build --tag language/node:20.10.0 --file containers/language/node/20.10.0/Dockerfile .

test:
  enabled: false
