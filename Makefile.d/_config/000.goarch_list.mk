#
# Makefile.d/_config/000.goos_list.mk
# (c) Sam Caldwell.  See LICENSE.txt
#
# This is the list of CPU Architectures for which we will
# build artifacts when we run 'make build' and similar
# operations against golang.
#
GOARCH_LIST := amd64 arm64
