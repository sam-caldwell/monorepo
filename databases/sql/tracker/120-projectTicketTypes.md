Project Ticket Types
====================

## Objectives
* Given a collection of `projects` and a collection of `ticketTypes`, this table will associate a given `project`
  with the subset of `ticketTypes` supported by that project.
