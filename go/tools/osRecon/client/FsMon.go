package client

func (app *Application) FsMon() error {
	// This go routine monitors the file system and disk storage systems and
	// related structures and pushes events out to eventQueue.
	return nil
}
