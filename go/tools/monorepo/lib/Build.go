package monorepo

func Build(class, projects *string) (err error) {
	//projectList, err := GetProjectList(class, projects)
	//if err != nil {
	//	return err
	//}
	//for _, projectManifest := range projectList {
	//	ansi.Blue().Printf("Project Manifest: %s", projectManifest).LF().Reset()
	//projects, err := LoadManifest(&projectManifest)
	//if err != nil {
	//	return err
	//}
	//for _, project := range projects {
	//	ansi.Blue().Printf("project: %s", project).LF().Reset()
	//}
	//}
	return err
}
