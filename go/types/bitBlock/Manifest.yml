---
header:
  author: Sam Caldwell
  email: mail@samcaldwell.net
  description: |
    The bitBlock is part of CRSCE.
  opsys:
    - darwin
  arch:
    - amd64
clean:
  enabled: false

build:
  enabled: false

test:
  enabled: true
  steps:
    - enabled: true
      continueOnError: true
      command: "go test -v -failfast ./go/types/bitBlock/..."
