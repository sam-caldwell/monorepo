package AtlassianJira

import "github.com/sam-caldwell/monorepo/go/types"

// Read - read a given project from JIRA into the internal state
func (project *Project) Read(apiKey types.ApiKey, projectName string) (err error) {
	return err
}
