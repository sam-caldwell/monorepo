package words

/*
 * Darwin (macOS) commands
 */
const (
	Macos                = "macos"
	MacSysCtl            = "sysctl"
	MacSysCtlHwMemSize   = "hw.memsize"
	MacSysCtlVmSwapUsage = "vm.swapusage"

	MacVmStat = "vm_stat"
)
