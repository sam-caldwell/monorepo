package hijack

// Free - Revert patch for the given target if it exists.
//func (table *TrackerTable) Free(target interface{}) bool {
//	/*
//	 * Given a target, look up the target in the TrackerTable and
//	 * revert the patch.  Then return a boolean value which indicates
//	 * if a revert was needed (e.g. the target had been patched).
//	 */
//	return table.unpatchValue(reflect.ValueOf(target))
//}
