---
header:
  author: Sam Caldwell
  email: mail@samcaldwell.net
  description: |
    This Golang project provides a simpleLogger for short-term needs until observability/logging is
    completed.
  opsys:
    - darwin
  arch:
    - amd64
clean:
  enabled: false

build:
  enabled: false

test:
  enabled: true
  steps:
    - enabled: true
      continueOnError: true
      command: "go test -v -failfast ./go/observability/simpleLogger/..."
