---
header:
  author: Sam Caldwell
  email: mail@samcaldwell.net
  description: |
    This Golang project is used to store/manage/interact with key-value or tabular data
  opsys:
    - any
  arch:
    - any
clean:
  enabled: false
  comment: "reusable golang code projects don't require a clean/build stage"

build:
  enabled: false
  comment: "reusable golang code projects don't require a clean/build stage"

test:
  enabled: true
  steps:
    - enabled: true
      continueOnError: true
      command: "go test -v -failfast ./go/keyvalue/..."
