---
header:
  author: Sam Caldwell
  email: mail@samcaldwell.net
  description: |
    This golang project provides a variable type
  opsys:
    - darwin
    - linux
  arch:
    - amd64
clean:
  enabled: false

build:
  enabled: false

test:
  enabled: true
  steps:
    - enabled: true
      continueOnError: true
      command: "go test -v -failfast ./go/trees/vartype..."
